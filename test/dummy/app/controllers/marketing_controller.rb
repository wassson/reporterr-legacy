# frozen_string_literal: true

class MarketingController < ApplicationController
  def index
    # Rails.error.handle(StandardError, severity: :info, context: { user_id: 3 } ) do
    # Rails.error.handle(StandardError) do
    #   puts 'Hello!'
    #   raise StandardError
    # end
  end
end
